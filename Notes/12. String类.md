# String 

## 介绍

## 初始化

## 输入

## 基本操作

### 字符处理

- C++对字符的处理直接将C的库包含在其中即可

```cpp
//一般写法:
#include<ctype.h>
#include<string.h>
//C++推荐写法:在库的最前加上c标识，标识这是一个C语言的库
#include<cctype>
#include<cstring>
```

